rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla global: Permitir todo a usuarios autenticados
    // (Incluye collection "reservas", "master_data", etc.)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
